<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings for PostgreSQL -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://[your_server]/[your_db]</property>
        <property name="connection.username">[your_user]</property>
        <property name="connection.password">[your_password]</property>
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- Enable Hibernate's automatic session context management -->
        <!--  thread is the short name for org.hibernate.context.ThreadLocalSessionContext and let Hibernate bind the session automatically to the thread -->
        <property name="current_session_context_class">thread</property>
        <property name="transaction.factory_class">org.hibernate.transaction.JDBCTransactionFactory</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <!-- this will show us all sql statements -->
        <property name="hibernate.show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <!--
        <property name="hbm2ddl.auto">create</property>
        -->

        <!-- Check the database schema on startup -->
        <property name="hbm2ddl.auto">validate</property>

        <!--
        <property name="hbm2ddl.auto">update</property>
        -->

        <!--
        -->
        <mapping class="org.hypothesis.entity.SlideTemplate"/>
        <mapping class="org.hypothesis.entity.SlideContent"/>
        <mapping class="org.hypothesis.entity.Slide"/>
        <mapping class="org.hypothesis.entity.SlideOutput"/>
        <mapping class="org.hypothesis.entity.Task"/>
        <mapping class="org.hypothesis.entity.Branch"/>
        <mapping class="org.hypothesis.entity.BranchOutput"/>
        <mapping class="org.hypothesis.entity.Pack"/>
        <mapping class="org.hypothesis.entity.BranchTrek"/>
        <mapping class="org.hypothesis.entity.Event"/>
        <mapping class="org.hypothesis.entity.Test"/>
        <mapping class="org.hypothesis.entity.Token"/>
        <mapping class="org.hypothesis.entity.User"/>
        <mapping class="org.hypothesis.entity.Group"/>
        <mapping class="org.hypothesis.entity.Role"/>
        <mapping class="org.hypothesis.entity.UserPermition"/>
        <mapping class="org.hypothesis.entity.GroupPermition"/>

        <!-- mapping files
        <mapping resource="de/laliluna/example/Honey.hbm.xml" />
        -->

    </session-factory>

</hibernate-configuration>
